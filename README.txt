# CSC1103
Programing Methodology mini project

Windows:
To compile the game(object files already exist), double click run.bat
TO compile the game(without object files), run Makefile using Make in cmd
To run the game, double click tic.exe

